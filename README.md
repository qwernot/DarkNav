<img width="1919" height="986" alt="image" src="https://github.com/user-attachments/assets/559901dc-6322-4c73-874d-db9e74868551" />

# Dark's Nav - Docker 部署教程

这是一个基于 React + Node.js 的极简导航页，支持 Docker 一键部署。数据保存在本地 `data.json` 文件中，支持持久化存储。

## 📋 前置要求

请确保您的服务器已安装：
- **Docker**
- **Docker Compose**

## 🚀 快速部署

### 第一步：准备文件
将项目所有文件上传至服务器的一个文件夹中（例如 `/home/darks-nav`）。
确保目录结构包含以下核心文件：
- `Dockerfile`
- `docker-compose.yml`
- `package.json`
- `server.js`
- `src/` (源代码目录)
- ...其他配置文件

### 第二步：初始化数据文件 (⚠️ 重要)
在启动容器之前，**必须**在宿主机手动创建一个空的 `data.json` 文件。如果不执行此步骤，Docker 会错误地将 `data.json` 创建为一个文件夹，导致程序报错。

在项目根目录下执行：
```bash
echo "{}" > data.json
```

### 第三步：启动服务
运行以下命令构建镜像并启动容器：
```bash
docker-compose up -d --build
```

### 第四步：访问网站
部署完成后，打开浏览器访问：
http://你的服务器IP:3000

## ⚙️ 管理与使用

### 管理员登录
点击侧边栏底部的用户图标进行登录。
默认密码：666333
登录后可添加分类、编辑链接、拖拽排序（未来功能）以及删除内容。

### 数据备份与迁移
所有导航数据都存储在项目根目录下的 `data.json` 文件中。
- 备份：只需复制 `data.json` 文件。
- 迁移：将备份的 `data.json` 放入新服务器的项目目录，重新运行容器即可。

### 更新网站
如果您修改了代码或获取了新版本：
1. 停止旧容器：
```bash
docker-compose down
```
2. 上传新的代码文件（保留 `data.json` 不动）。
3. 重新构建并启动：
```bash
docker-compose up -d --build
```

## 🛠 常见问题排查

### Q：部署后打开是白屏？
A: 请检查浏览器控制台（F12 -> Console）。如果是语法错误或模块错误，请确保 `index.html` 中没有残留的导入映射代码，并且 `vite.config.ts` 中的配置设置正确。

### Q：报错“地理位置权限被拒绝”？
A: 浏览器禁止在非 HTTPS 环境下使用精准 GPS。程序会自动降级使用 IP 定位（显示“本地”或城市名）。

### Q：报错“EISDIR：对目录进行非法操作，打开‘data.json’”？
A: 这是因为您忘记执行“第二步：初始化数据文件”。请删除自动生成的 `data.json` 文件夹，并执行 `echo "{}" > data.json`。
